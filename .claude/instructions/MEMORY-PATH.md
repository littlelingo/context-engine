# Agent Memory Conventions

Agent memory lives at `.claude/agent-memory/` relative to the **git root** — never create `.claude/` directories inside subdirectories.

Read your memory first — you may already know where things are. Update it with new discoveries before returning.
